<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="ufc.smd.esqueleto_placar">

    <uses-permission android:name="android.permission.VIBRATE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_volei_foreground"
        android:label="PlacarVolei"
        android:roundIcon="@mipmap/ic_volei_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Esqueleto_Placar">
        <activity
            android:name=".PlacarActivity"
            android:exported="false" />
        <activity
            android:name=".PreviousGamesActivity"
            android:exported="false" />
        <activity
            android:name=".ConfigActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>